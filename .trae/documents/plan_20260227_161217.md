Vou implementar as novas funcionalidades de "Sortear" e "Ranking" no Gerenciador da Campanha, conforme solicitado.

### 1. Criar Componentes
Criarei dois novos componentes modulares em `src/components`:
- **`RaffleManager.tsx`**: Implementará a interface de sorteio mostrada na Imagem 2.
  - Seleção de Canal de Sorteio (Loteria Federal).
  - Input para adicionar números de bilhetes premiados.
  - Lista de bilhetes adicionados.
- **`RankingManager.tsx`**: Implementará a interface de ranking (placeholder inicial, já que não há imagem detalhada, mas preparado para expansão).

### 2. Atualizar o Gerenciador de Campanhas
Editarei o arquivo `src/pages/CampaignManager.tsx` para:
- Importar os novos componentes.
- Adicionar os cards "Ranking" e "Sortear" ao menu de navegação.
- Criar a lógica de navegação (tabs) para exibir os componentes `RaffleManager` e `RankingManager` quando seus respectivos cards forem clicados.

### 3. Detalhes da Implementação
- **Estilo**: Manterei o padrão visual escuro (Dark Mode) usando Tailwind CSS, consistente com o restante da aplicação.
- **Ícones**: Utilizarei Material Icons conforme o padrão do projeto.
- **Modularidade**: A lógica será separada em arquivos menores para facilitar a manutenção, atendendo à regra de "separar arquivos extensos".

### Arquivos Afetados
- `src/components/RaffleManager.tsx` (Novo)
- `src/components/RankingManager.tsx` (Novo)
- `src/pages/CampaignManager.tsx` (Edição)