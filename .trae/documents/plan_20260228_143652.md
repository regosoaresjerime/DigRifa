Vou implementar o design de celebração e a animação de confetes na página pública da rifa quando a campanha estiver finalizada, seguindo rigorosamente as imagens fornecidas e a skill de Frontend Design.

1. **Animação de Confetes (CSS Puro)**:

   * Criarei uma animação de confetes vibrante e festiva usando apenas CSS e JavaScript leve para gerenciar o loop de 8 segundos.

   * Os confetes "explodirão" do topo da tela, criando o efeito de festa solicitado.

2. **Layout de "Campanha Finalizada"**:

   * Modificarei o `RafflePage.tsx` para detectar quando a campanha tiver o status 'Finalizada' (ou tiver resultados de sorteio preenchidos).

   * **Hero Section (Topo)**: Substituirei a área de compra por um card de destaque **Verde Vibrante** (#22c55e) com um ícone de troféu e o texto "Campanha Finalizada", exatamente como na referência.

   * **Seção de Bilhetes Sorteados**:

     * Criarei cards escuros (Dark Mode) com bordas sutis para listar os bilhetes vencedores.

     * Cada card exibirá o número do bilhete em destaque (verde) e o prêmio correspondente.

   * **Seção de Ganhadores**:

     * Exibirei a lista de ganhadores com nome e prêmio, mantendo o design limpo e profissional.

3. **Implementação Técnica**:

   * **Arquivo**: `src/pages/RafflePage.tsx`.

   * **Lógica**: Adicionarei uma verificação `if (campaign.status === 'Finalizada')` (ou verificando se há `drawResults`).

   * **Dados**: Buscarei os nomes dos ganhadores cruzando os números sorteados (`drawResults`) com o histórico de compras (`purchase_history`), para exibir "Quem ganhou" publicamente.

4. **Estilo e Acabamento (Frontend Design)**:

   * **Tipografia**: Usarei fontes sem serifa, pesos fortes (Bold/Black) para títulos e cores de alto contraste.

   * **Cores**: Fundo escuro (#121212), Card Verde (#22c55e), Texto Branco/Cinza Claro.

   * \*\*Respons

