I will implement the requested Webhook functionality in `RaffleManager.tsx` with the following plan:

1. **Update Interface**: Modify `RaffleManagerProps` and the internal `Campaign` interface in `RaffleManager.tsx` to include `ticketQuantity: number`. This ensures we have access to the total tickets to calculate the interval.

2. **Add State Management**:

   * `webhookUrl`: To store the current webhook URL.

   * `isWebhookModalOpen`: To toggle the visibility of the floating card (modal).

   * `loading`: To show loading state during the "Extração IA" request.

3. **Persistence**:

   * Use `localStorage` to save and retrieve the `webhookUrl` (key: `digrifa_webhook_url`), allowing it to persist across sessions and be editable at any time.

4. **UI Implementation**:

   * Add a **"Webhook"** button next to the "Extração IA" button.

   * Create a **Floating Card (Modal)** component that appears when the "Webhook" button is clicked. It will contain:

     * Input field for the Webhook URL.

     * "Salvar" (Save) button.

     * Close button.

5. **Logic Implementation**:

   * **Interval Calculation**: Create a helper function to determine the `interval_start` and `interval_end` based on `ticketQuantity`.

     * Start is always `0` padded to the length of `ticketQuantity - 1`.

     * End is `ticketQuantity - 1` padded to the same length.

   * **"Extração IA" Handler**:

     * Check if `webhookUrl` is set. If not, alert the user to configure it.

     * Construct the JSON payload:

       ```json
       {
         "campaign_name": campaign.title,
         "total_tickets": campaign.ticketQuantity,
         "interval_start": "000...", 
         "interval_end": "999..."
       }
       ```

     * Send a POST request to the `webhookUrl` with this payload.

     * Provide feedback (success/error toast or alert).

6. **Style**: Maintain consistency with the existing Dark Mode UI using Tailwind CSS.

### Files to Modify

* `src/components/RaffleManager.tsx`

